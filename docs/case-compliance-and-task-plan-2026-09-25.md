# Аудит соответствия кейсу, ROADMAP и план задач (25.09.2026)

Дата: 25 сентября 2026 года  
Базовая ветка: `origin/develop` (коммит `c28e2e7` после PR #49); реализация ниже находится в `codex/python-server-runner` до merge
Источники:
- [Кейс хакатона (PDF)](../keys-obrazovatelnaya-platforma.pdf)
- [Роадмап проекта](../ROADMAP.md)
- [Материалы организатора](organizer/README.md)
- [Карта 30 шагов](organizer-step-map.md)
- [План демо для одного агента](single-agent-demo-plan.md)

---

## 1. Текущий срез репозитория

- **Git-синхронизация**: Ветка `codex/python-server-runner` обновлена до `origin/develop` (`c28e2e7`). PR #47, #48 и #49 влиты.
- **Тесты backend**: 72 тест Django проходит в ветке runner (`python manage.py test`).
- **Preflight импорта**: Валидация 3 курсов / 9 модулей / 30 шагов проходит успешно (`preflight_curriculum_import`).
- **Тесты frontend**: 75 тестов Vitest проходят в ветке runner, TypeScript (`tsc --noEmit`) и сборка Vite завершаются без ошибок.
- **Инфраструктура**: Docker Compose включает `db`, `web`, `proxy` и изолированный `runner`; Nginx, healthcheck и backup/restore готовы. Запуск контейнеров на текущей машине не проверен: Docker отсутствует.

---

## 2. Сверка текущего состояния проекта с кейсом (PDF)

| Раздел кейса | Требование кейса | Состояние в проекте | Статус |
|---|---|---|---|
| **§03. Три роли, три контура** | **Ученик**: доступные курсы, текущее положение, следующий шаг, история сданных работ, обратная связь.<br>**Куратор**: список закрепленных учеников, очередь работ на ручную проверку, возврат с комментарием, ответ на вопрос по конкретному шагу.<br>**Администратор**: создание и публикация курсов, сборка из шагов, назначение кураторов и учеников, изменение курсов. | Все 3 кабинета реализованы в интерфейсе и API. Сессионная аутентификация, строгие DRF permissions, неизменяемые опубликованные ревизии `CourseRevision`. Решение не ограничивается кабинетом ученика. | **Полное соответствие ядру** |
| **§04. Устройство курса** | Курс собирается из разнотипных шагов. Специфические шаги: **Scratch** (разбор конструкции, число/ссылка), **Minecraft Education** (задание в среде, снимок/мир), **Алгоритмика** (прогон по тестам), **Основа любого курса** (теория и контрольный вопрос). Расширяемая архитектура типов. | Реализован расширяемый реестр из 9 типов шагов (`theory`, `quiz.single_choice`, `quiz.multiple_choice`, `answer.exact`, `scratch.numeric_answer`, `algorithm.python`, `artifact.scratch`, `artifact.minecraft`, `artifact.project`). Функция `publish_course` строго проверяет наличие теории и контрольного вопроса.<br>⚠️ Реальные 30 шагов пока существуют в карте `curriculum-map.json`, но не загружены в БД. | **Архитектурно готово; блокируется импортом в БД** |
| **§05. Проверка заданий** | **Автоматическая**: тест, задача с ответом, алгоритмическая задача с прогоном по тестам (результат сразу).<br>**Ручная**: проект, файл, ссылка; очередь куратора, возврат с обязательным комментарием, статус и причина возврата у ученика. | Автопроверка тестов и точных ответов работает на сервере. Подсказка Scratch 1.1.3 возвращается только после неверной сдачи. Составные доказательства (файл, URL, пояснение) и критерии проверки куратора поддержаны.<br>В ветке runner Pyodide оставлен только для локального примера, официальный код исполняется на сервере в отдельном контейнере. Поддельный клиентский `stdout` отклоняется. Compose-проверка на целевом хосте остаётся открытой. | **Реализовано в feature-ветке; нужна стендовая проверка** |
| **§06. Прогресс и рейтинг** | 1) Ученик видит свое положение и следующий шаг.<br>2) Рейтинг объясним (понятно, из чего сложился результат).<br>3) Ручная проверка влияет на прогресс наравне с автопроверкой.<br>4) Куратор видит отставание раньше, чем ученик перестанет заходить. | 1) Выделен блок следующего шага в `course-hero`.<br>2) Прогресс содержит разбивку баллов по шагам и процент выполнения.<br>3) Баллы за ручную проверку начисляются сервером при принятии работы.<br>4) В кабинете куратора работают объяснимые сигналы застоя (72 ч без зачета, 2 неверные попытки за 24 ч, возврат без пересдачи за 24 ч). | **Полное соответствие** |
| **§07, §11. Ограничения** | Запрещены no-code конструкторы, сторонний продуктовый AI, реальные персональные данные. Исключены лишние надстройки (CRM, платежи, видеосвязь, общий чат). | Проект написан полностью кодом, AI внутри продукта отсутствует, используются исключительно синтетические аккаунты (`admin_demo`, `student_demo`, `curator_demo`), лишние надстройки исключены. Лицензия MIT включена. | **Полное соответствие** |
| **§09. Материалы организатора** | Перенести базовый учебный пакет (3 курса / 9 модулей / 30 шагов) и применить официальный брендбук. | Брендбук перенесен в токены и верстку UI. Карта 30 шагов и preflight валидируют оригинальный DOCX. В БД курсы еще не импортированы из-за отсутствия сущности модуля и паспорта в модели `Course`. | **Блокер сдачи (P0)** |
| **§10. Развернутый стенд** | Решение должно открываться по ссылке в браузере к стоп-коду 26 сентября 09:00. | Локальный стенд и Compose-файлы готовы, но внешний публичный стенд (HTTPS через Cloudflare Tunnel) пока не развернут. | **Блокер сдачи (P0)** |

---

## 3. Сверка с роадмапом (`ROADMAP.md`)

- **Этап 0 (Каркас, P0)**: **100% готов** (архитектура, API, Django, React, Compose, demo-accounts).
- **Этап 1 (Данные и авторизация, P0)**: **95% готов** (роли, неизменяемые ревизии, правило теории и контрольного вопроса). Не хватает полей паспорта курса и модулей.
- **Этап 2 (Расширяемые шаги и прохождение, P0)**: **90% готов** (все 9 типов шагов поддержаны в валидаторах и формах). Не хватает импортированных данных в БД.
- **Этап 3 (Проверки, P0)**: **Реализовано в feature-ветке** (серверная Python-автопроверка, составные артефакты, подсказка Scratch, очередь куратора). Нужны merge и проверка runner в Compose.
- **Этап 4 (Куратор и прогресс, P0)**: **95% готов** (очередь, возврат с обязательным комментарием, контекстные вопросы к шагу, сигналы застоя, объяснимый рейтинг).
- **Этап 5 (Демо, тестирование и стенд, P0)**: **В процессе**. Готовы тесты прав, CI, preflight. Открыты: импорт 3/9/30, Compose-приёмка runner, публичный стенд (Cloudflare Tunnel), сквозной E2E и релизный PR в `main`.
- **Этапы 6–7 (P1 / P2)**: Отложены до полной готовности ядра и стенда.

---

## 4. Концепция решения для Python (Таска 1)

Согласно требованиям и решению команды:
1. **Локальное исполнение в браузере (самопроверка)**:
   - Сохраняется для мгновенного отклика и удобной отладки учеником без ожидания очереди.
   - Выполняется через Web Worker (Pyodide).
   - Запускается по кнопке **«Проверить локально»**.
   - **Не отправляет сдачу на сервер, не создает записей `Submission` и не начисляет баллы**.
   - Показывает ученику фактический `stdout`, ошибки выполнения и время запуска на открытых тестах/примерах.
2. **Официальная сдача решения**:
   - По кнопке **«Отправить на проверку»** фронтенд отправляет на сервер исключительно код: `{"code": code}`.
   - Браузерные результаты (`stdout`, `duration_ms`, `peak_memory_bytes`) сервером не принимаются и не запрашиваются.
   - Сервер самостоятельно выполняет код в изолированной среде:
     - Ограничение времени CPU и wall-clock timeout (`time_limit_ms`).
     - Ограничение оперативной памяти (`memory_limit_mb`).
     - Ограничение размера вывода (`output_limit_bytes`).
     - Изолированный временный каталог без доступа к секретам проекта, БД и внешней сети.
     - Сопоставление с эталонным выводом закрытых тестов.
   - Сервер выставляет официальный статус (`accepted`, `incorrect` или `error`), начисляет баллы и возвращает безопасную диагностику (число пройденных тестов и причину непрохождения).

---

## 5. Очередь задач (Критический путь)

Задачи выполняются строго последовательно по **GitFlow**: каждая задача реализуется в ветке `codex/<name>`, покрывается тестами и вливается в `develop` через Pull Request.

```text
[P0-1: Серверный Python-раннер и UI самопроверки]
                      ↓
[P0-2: Модель модулей, паспорт курса и импорт пакета 3/9/30]
                      ↓
[P0-3: Публичный стенд Docker Compose + Cloudflare Tunnel]
                      ↓
[P0-4: Сквозной E2E-сценарий трех ролей на реальных курсах]
                      ↓
[P0-5: Обновление README и релиз в ветку main до стоп-кода]
```

### Таска 1. Изолированный серверный runner Python и разделение самопроверки

**Статус:** реализовано в `codex/python-server-runner`, [PR #50](https://github.com/D0UP1G/WebEducation/pull/50) открыт в `develop`. Локальный Docker Compose с браузерной самопроверкой и официальными исходами `wrong_answer`, `time_limit`, `accepted` пройден. После двух стендовых исправлений CI запущен повторно; merge ещё открыт. Публичный HTTPS-стенд относится к таске 3. [Подробный аудит](python-runner-audit-2026-09-25.md).

- **Ветка**: `codex/python-server-runner`
- **Backend**:
  - Изменить `PythonSubmissionInput`: принимать только `code` (убрать обязательные браузерные `results` и `challenge_token`).
  - Реализовать изолированный модуль выполнения Python-кода (запуск подпроцесса с лимитами времени, памяти через `resource.setrlimit`, минимальным окружением без секретов, временной директорией).
  - Сверять вывод со всеми тестами задания, формировать статус `accepted` / `incorrect` / `error` и безопасные `safe_diagnostics`.
  - Добавить тест, подтверждающий, что поддельный ввод/вывод не дает зачета, а корректный код исполняется сервером.
- **Frontend**:
  - Кнопка «Проверить локально»: запускает Pyodide в воркере и показывает локальный результат самопроверки прямо на странице без создания `Submission`.
  - Кнопка «Отправить на проверку»: отправляет `{ code }` на сервер, переводит карточку в состояние ожидания/проверки и отображает итоговый серверный статус и баллы.

### Таска 2. Модель модулей, паспорт курса и импорт пакета 3/9/30
- **Ветка**: `codex/curriculum-import`
- **Backend**:
  - Добавить в `Course` и `CourseRevision` поля паспорта (`tool`, `goal`, `volume`, `source_id`).
  - Добавить сущность `Module` / `ModuleRevision` для сохранения 9 модулей.
  - На базе готового `backend/apps/courses/import_plan.py` реализовать команду `import_curriculum` для загрузки 3 курсов / 9 модулей / 30 шагов.
  - Обновить `seed_demo.py`, чтобы при инициализации базы сразу создавались и публиковались все 3 курса с назначенными 30 баллами.

### Таска 3. Запуск Compose и развертывание Cloudflare Tunnel
- **Ветка**: `codex/tunnel-deployment`
- **Инфраструктура**:
  - Поднять `docker-compose.yml` на стенде (`web`, `db`, `proxy`, `runner`).
  - Настроить `cloudflared tunnel` на локальный порт Nginx `127.0.0.1:8080`.
  - Настроить безопасный `.env` (продакшн-секреты, домен туннеля в `DJANGO_ALLOWED_HOSTS` и `DJANGO_CSRF_TRUSTED_ORIGINS`).

### Таска 4. Сквозной E2E-прогон трех ролей на импортированных материалах
- **Действия**:
  1. Администратор: публикация версий, назначение курсов.
  2. Ученик: прохождение теории, ответов на вопросы, локальная самопроверка и официальная сдача Python, сдача проектов Scratch и Minecraft с файлом и ссылкой.
  3. Куратор: проверка очереди, возврат работы с обязательным комментарием, ответ на контекстный вопрос, прием пересдачи.
  4. Проверка обновления прогресса ученика и фиксации версии при повторной публикации.

### Таска 5. Финализация документации и релиз в `main`
- **Ветка**: `release/demo-ready`
- **Действия**:
  - Зафиксировать в `README.md` публичный URL, учетные записи демо-доступа, инструкцию сборки и статус выполненных задач.
  - Открыть Pull Request из `release/demo-ready` (или `develop`) в `main`.
  - Убедиться в прохождении всех проверок CI и смержить до стоп-кода 26 сентября 09:00.
