# Аудит соответствия кейсу, ROADMAP и план задач (25.09.2026)

Дата обновления: 25 сентября 2026 года
База аудита: `develop` после merge PR #52 (`dedb882`). Последующее изменение в
`feature/stepwise-python-editor` добавляет последовательную разблокировку шагов
и редактор Python; ветка ожидает PR и не считается частью `develop` до merge.
Источники:
- [Кейс хакатона (PDF)](../keys-obrazovatelnaya-platforma.pdf)
- [Роадмап проекта](../ROADMAP.md)
- [Материалы организатора](organizer/README.md)
- [Карта 30 шагов](organizer-step-map.md)
- [План демо для одного агента](single-agent-demo-plan.md)

---

## 1. Текущий срез репозитория

- **Git-синхронизация**: PR #52 влит в `develop` (`dedb882`). Ветка `feature/stepwise-python-editor` основана на этом состоянии.
- **Проверки feature-ветки**: 78 backend-тестов, 80 frontend-тестов, TypeScript и production build прошли; runner unit tests — 2/2.
- **Учебный пакет**: preflight и `seed_demo` проверены на чистой SQLite-базе: 3 курса / 9 модулей / 30 шагов, 30 баллов; созданы 3 демо-роли и 3 чистых назначения, попыток/вопросов/проверок нет; повторный импорт сообщил `published_courses: 0`.
- **Compose**: основной конфиг проходит `docker compose config --quiet`; PR CI до синхронизации показывал зелёные backend, frontend и Compose jobs.
- **Инфраструктура**: Compose включает `db`, `web`, `proxy` и изолированный `runner`; локальный Docker-прогон проходит. Публичный HTTPS-стенд и полный E2E остаются открытыми.
- **Ручная локальная проверка 25.09**: в Vite-потоке теория и контрольный вопрос по очереди открыли следующую задачу; последующие шаги оставались закрыты. В Python-редакторе проверены автозакрытие скобок, автоотступ, Tab и подсказка `print`; Pyodide решил открытый пример `2 3 → 5`. Строку с клавиатурными подсказками убрали по просьбе команды.
- **Состояние запущенного стенда**: доступный по LAN Compose пока показывает старую сборку и прежний синтетический курс. Django dev server на `8000` не подключён к runner-сокету, поэтому официальная сдача через него ожидаемо недоступна. Старый HTTP LAN-клиент падал на `crypto.randomUUID()`; в текущую feature-ветку добавлен fallback UUID. Для проверки текущей сборки с runner нужно пересобрать Compose и выполнить `seed_demo`; Docker socket требует `sudo`.

---

## 2. Сверка текущего состояния проекта с кейсом (PDF)

| Раздел кейса | Требование кейса | Состояние в проекте | Статус |
|---|---|---|---|
| **§03. Три роли, три контура** | **Ученик**: доступные курсы, текущее положение, следующий шаг, история сданных работ, обратная связь.<br>**Куратор**: список закрепленных учеников, очередь работ на ручную проверку, возврат с комментарием, ответ на вопрос по конкретному шагу.<br>**Администратор**: создание и публикация курсов, сборка из шагов, назначение кураторов и учеников, изменение курсов. | Все 3 кабинета реализованы в интерфейсе и API. Сессионная аутентификация, строгие DRF permissions, неизменяемые опубликованные ревизии `CourseRevision`. Решение не ограничивается кабинетом ученика. | **Полное соответствие ядру** |
| **§04. Устройство курса** | Курс собирается из разнотипных шагов. Специфические шаги: **Scratch** (разбор конструкции, число/ссылка), **Minecraft Education** (задание в среде, снимок/мир), **Алгоритмика** (прогон по тестам), **Основа любого курса** (теория и контрольный вопрос). Расширяемая архитектура типов. | Реестр 9 типов, структура 3/9/30 и отображение модулей реализованы в PR #52. Feature `stepwise-python-editor` добавляет продвижение только после зачёта каждого предыдущего шага. | **Порядок реализован и проверен тестами; ждёт PR и E2E на импортированных данных** |
| **§05. Проверка заданий** | **Автоматическая**: тест, задача с ответом, алгоритмическая задача с прогоном по тестам (результат сразу).<br>**Ручная**: проект, файл, ссылка; очередь куратора, возврат с обязательным комментарием, статус и причина возврата у ученика. | Автопроверка тестов и точных ответов работает на сервере. Подсказка Scratch 1.1.3 возвращается только после неверной сдачи. Составные доказательства и критерии куратора поддержаны. PR #50 исполняет Python-код в изолированном серверном runner; Pyodide делает локальную самопроверку. Повторные варианты Python проходят серверную оценку, не отменяя уже полученный зачёт. | **Реализовано; нужна приёмка на импортированном курсе и публичном стенде** |
| **§06. Прогресс и рейтинг** | 1) Ученик видит свое положение и следующий шаг.<br>2) Рейтинг объясним (понятно, из чего сложился результат).<br>3) Ручная проверка влияет на прогресс наравне с автопроверкой.<br>4) Куратор видит отставание раньше, чем ученик перестанет заходить. | 1) Выделен блок следующего шага в `course-hero`.<br>2) Прогресс содержит разбивку баллов по шагам и процент выполнения.<br>3) Баллы за ручную проверку начисляются сервером при принятии работы.<br>4) В кабинете куратора работают объяснимые сигналы застоя (72 ч без зачета, 2 неверные попытки за 24 ч, возврат без пересдачи за 24 ч). | **Полное соответствие** |
| **§07, §11. Ограничения** | Запрещены no-code конструкторы, сторонний продуктовый AI, реальные персональные данные. Исключены лишние надстройки (CRM, платежи, видеосвязь, общий чат). | Проект написан полностью кодом, AI внутри продукта отсутствует, используются исключительно синтетические аккаунты (`admin_demo`, `student_demo`, `curator_demo`), лишние надстройки исключены. Лицензия MIT включена. | **Полное соответствие** |
| **§09. Материалы организатора** | Перенести базовый учебный пакет (3 курса / 9 модулей / 30 шагов) и применить официальный брендбук. | Брендбук отражён в токенах и UI. PR #51 добавил миграцию, паспорт курса, модули и идемпотентный импорт; PR #52 убрал тестовый курс и показывает паспорт с группировкой. | **Пакет готов; остаётся сквозная приёмка** |
| **§10. Развернутый стенд** | Решение должно открываться по ссылке в браузере к стоп-коду 26 сентября 09:00. | Локальный стенд и Compose-файлы готовы, но внешний публичный стенд (HTTPS через Cloudflare Tunnel) пока не развернут. | **Блокер сдачи (P0)** |

---

## 3. Сверка с роадмапом (`ROADMAP.md`)

- **Этап 0 (Каркас, P0)**: **100% готов** (архитектура, API, Django, React, Compose, demo-accounts).
- **Этап 1 (Данные и авторизация, P0)**: **Реализовано**; PR #51 добавил паспорт курса, модульную структуру и стабильные ключи источника в черновики и версии.
- **Этап 2 (Расширяемые шаги и прохождение, P0)**: структура и пакет 3/9/30 реализованы в PR #52; последовательное открытие по `accepted` проверено в feature и ожидает merge.
- **Этап 3 (Проверки, P0)**: **Реализовано в PR #50** (серверная Python-автопроверка, составные артефакты, подсказка Scratch, очередь куратора). Локальный Compose и CI пройдены; остаётся E2E на реальных курсах.
- **Этап 4 (Куратор и прогресс, P0)**: **95% готов** (очередь, возврат с обязательным комментарием, контекстные вопросы к шагу, сигналы застоя, объяснимый рейтинг).
- **Этап 5 (Демо, тестирование и стенд, P0)**: **В процессе**. Открыты: полный E2E трёх ролей на импортированном пакете, внешний HTTPS через Cloudflare Tunnel, проверка восстановления на стенде и релиз в `main`.
- **Этапы 6–7 (P1 / P2)**: Отложены до полной готовности ядра и стенда.

---

## 4. Концепция решения для Python (Таска 1)

Согласно требованиям и решению команды:
1. **Локальное исполнение в браузере (самопроверка)**:
   - Сохраняется для мгновенного отклика и удобной отладки учеником без ожидания очереди.
   - Выполняется через Web Worker (Pyodide).
   - Запускается по кнопке **«Проверить локально»**.
   - **Не отправляет сдачу на сервер, не создает записей `Submission` и не начисляет баллы**.
   - Показывает ученику фактический `stdout`, ошибки выполнения и время запуска на открытых тестах/примерах.
2. **Официальная сдача решения**:
   - По кнопке **«Отправить на проверку»** фронтенд отправляет на сервер исключительно код: `{"code": code}`.
   - Браузерные результаты (`stdout`, `duration_ms`, `peak_memory_bytes`) сервером не принимаются и не запрашиваются.
   - Сервер самостоятельно выполняет код в изолированной среде:
     - Ограничение времени CPU и wall-clock timeout (`time_limit_ms`).
     - Ограничение оперативной памяти (`memory_limit_mb`).
     - Ограничение размера вывода (`output_limit_bytes`).
     - Изолированный временный каталог без доступа к секретам проекта, БД и внешней сети.
     - Сопоставление с эталонным выводом закрытых тестов.
   - Сервер выставляет официальный статус (`accepted`, `incorrect` или `error`), начисляет баллы и возвращает безопасную диагностику (число пройденных тестов и причину непрохождения).

---

## 5. Очередь задач (Критический путь)

Задачи выполняются строго последовательно по **GitFlow**: каждая задача реализуется в ветке `codex/<name>`, покрывается тестами и вливается в `develop` через Pull Request.

```text
[P0-1: Полный E2E трёх ролей на пакетах 3/9/30]
                      ↓
[P0-2: Публичный стенд Docker Compose + Cloudflare Tunnel]
                      ↓
[P0-3: Приёмка восстановления данных и финализация README]
                      ↓
[P0-4: Release PR в main]
```

### Таска 1. Изолированный серверный runner Python и разделение самопроверки

**Статус:** PR #50 влит в `develop`. Локальный Docker Compose с браузерной самопроверкой и исходами `wrong_answer`, `time_limit`, `accepted` пройден; новые варианты Python после зачёта также проходят серверную проверку, а прежний зачёт и баллы сохраняются. Публичный HTTPS-стенд и E2E на официальном пакете остаются открытыми. [Подробный аудит](python-runner-audit-2026-09-25.md).

- **Ветка**: `codex/python-server-runner`
PR #50 разделил локальную самопроверку через Pyodide и официальную сдачу: API принимает только код и отправляет его в изолированный runner. Runner применяет временные и ресурсные лимиты, сравнивает все тесты и возвращает безопасную диагностику; клиентский `stdout` на зачёт не влияет.

### Таска 2. Модель модулей, паспорт курса и импорт пакета 3/9/30
**Статус:** модель и импорт реализованы в PR #51, официальный seed и отображение модулей — в PR #52. Добавлены миграция `0002_curriculum_structure`, поля паспорта, `Module`/`ModuleRevision` и транзакционный импорт карты по стабильным `source_id`. Команда `import_curriculum --publish` создаёт/обновляет три курса; `seed_demo` оставляет только официальный пакет и чистые назначения, без синтетического курса и фиктивных сдач. Тесты проверяют суммы 10/8/12, группировку, повторный импорт и чистый seed.

### Таска 6. Поэтапное прохождение и Python-редактор
**Статус:** реализация и локальные проверки прошли в `feature/stepwise-python-editor`; PR ещё не создан и merge не выполнен.

- Доступ к каждому шагу, отправкам, вопросам и Python-примеру проверяется на
  сервере: все шаги с меньшей позицией должны иметь статус `accepted`.
- Enrollment выдаёт только метаданные списка шагов; содержимое загружается по
  отдельному endpoint для открытого шага. В прогрессе появляется `unlocked`.
- В ученическом курсе будущие шаги видны как закрытые; pending review также
  блокирует продвижение.
- CodeMirror 6 предоставляет Python-подсветку, встроенные и локальные подсказки,
  сниппеты, автозакрытие скобок и кавычек, автоотступ и Tab для отступа.
- В ручном Vite-прогоне проверены продвижение после зачёта теории и вопроса,
  автозакрытие `[]`, Tab/автоотступ, `Ctrl+Space` с подсказкой `print` и локальный
  запуск открытого Python-примера. Видимую строку клавиатурной помощи удалили.
- Fallback генерации UUID добавлен для HTTP-страниц в LAN, где недоступен
  `crypto.randomUUID()`. Перед приёмкой необходимо пересобрать текущий Compose,
  запустить `seed_demo`, затем проверить серверную Python-сдачу и полный
  сценарий на импортированном курсе. Подсказки CodeMirror не являются
  полноценным Python LSP.

### Таска 3. Сквозной E2E-прогон трех ролей на импортированных материалах
- **Действия**:
  1. Администратор: публикация версий, назначение курсов.
  2. Ученик: прохождение теории, ответов на вопросы, локальная самопроверка и официальная сдача Python, сдача проектов Scratch и Minecraft с файлом и ссылкой.
  3. Куратор: проверка очереди, возврат работы с обязательным комментарием, ответ на контекстный вопрос, прием пересдачи.
  4. Проверка обновления прогресса ученика и фиксации версии при повторной публикации.

### Таска 4. Развертывание Cloudflare Tunnel и внешняя приёмка
- **Следующая инфраструктурная задача:** развернуть Compose на целевом хосте, опубликовать только Nginx через Cloudflare Tunnel, задать production secrets и host/CSRF allowlists. Повторить E2E, проверить Python runner, загрузку/скачивание файлов и backup/restore.

### Таска 5. Финализация документации и релиз в `main`
- **Ветка**: `release/demo-ready`
- **Действия**:
  - Зафиксировать в `README.md` публичный URL, учетные записи демо-доступа, инструкцию сборки и статус выполненных задач.
  - Открыть Pull Request из `release/demo-ready` (или `develop`) в `main`.
  - Убедиться в прохождении всех проверок CI и смержить до стоп-кода 26 сентября 09:00.
