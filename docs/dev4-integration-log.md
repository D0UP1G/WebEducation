# DEV-4: журнал интеграции и аудита

Этот журнал фиксирует проверенный срез и результаты шагов переноса содержания.
Исторические записи не превращаются в подтверждение работы публичного стенда.

## 24 сентября 2026 — `develop` на `df202b2`

**Источники:** [кейс](../keys-obrazovatelnaya-platforma.pdf), §§04–06 и 09–12;
[архитектура](../ARCHITECTURE.md), §§6–13; [план команды](team-work-plan.md),
раздел DEV-4; [контракт импорта](organizer/import-contract.md); код `develop`.
Перед работой выполнен `git fetch origin`. Последние слияния: PR #34
(`abd8546`, рабочий React), PR #38 (`b7e0787`, handoff дизайна) и PR #37
(`df202b2`, карта и preflight импорта). Работа ведётся от `df202b2` в отдельной
ветке `codex/dev4-import-audit`.

| Проверка | Результат и свидетельство |
|---|---|
| Исходный DOCX и карта | SHA-256 файла совпал с `source.sha256` карты. Проверка стандартной библиотекой Python дала 3 курса, 9 модулей, 30 шагов, 30 шагов без `max_score` и 6 ручных шагов с критериями. Ответы и критерии в журнал не выводились. |
| Подготовка импорта | PR #37 уже в `develop`: `import_plan.py` строит план, `preflight_curriculum_import` валидирует его без записи в БД. Для этого среза локальный запуск Django-команды остановился на `ModuleNotFoundError: django` в доступном Python; проверка выше выполнена отдельно. Последний коммит PR #37 ранее прошёл backend/frontend CI. |
| Схема БД | `Course` и `DraftStep` в `df202b2` содержат плоские шаги. `Module`, структурированного паспорта и стабильных ключей источника в моделях нет. Сохранение 3/9/30 в эту схему потеряет группировку и не даст безопасного повторного импорта. |
| Ручные доказательства | Изменения PR #36 не входят в `df202b2`. Карта уже содержит `required_evidence` и закрытые критерии, но серверная проверка этих полей ожидает интеграции DEV-3. |
| Проверка кейса | §04 требует теорию и контрольный вопрос; все три курса карты их содержат. §09 требует переноса реального пакета, поэтому наличие карты не равно импорту. §11 разрешает только синтетические данные. Согласие законного представителя в §12 относится к участникам хакатона младше 18 лет, а не к регистрации учеников платформы; формулировки текущих сверок исправлены. |
| Публичная приёмка | HTTPS URL, импортированные draft/ревизии, официальный серверный Python-прогон и полный маршрут трёх ролей на этом срезе не подтверждены. |

## Протокол следующей приёмки

После схемы DEV-1, интеграции правил DEV-3 и решения о баллах DEV-4 запускает
транзакционный повторяемый импорт, публикует версии и назначает только
синтетические аккаунты. Журнал каждого прогона должен содержать дату и время,
коммит, URL стенда, SHA-256 исходного DOCX, количество созданных/обновлённых/
пропущенных записей и итог. Повторный запуск должен давать ноль дублей и не
изменять существующие назначения или попытки.

Для каждого перехода сквозного сценария записывать роль синтетического
пользователя, операцию/маршрут, HTTP-статус, наблюдаемый результат и
`X-Request-ID` либо `meta.request_id`. По этому идентификатору находить запись
API в request-id логах. Не помещать в журнал ответы, эталонные решения,
содержимое сдач, токены и персональные данные. Отдельно фиксировать проверку
старого назначения после новой публикации, отказ в зачёте по поддельному
клиентскому Python-результату, лимиты исполнителя и результат backup/restore.
