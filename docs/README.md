# Документация команды

Начинать работу следует с этого файла и документов в указанном порядке:

1. [Реестр недостатков MVP](mvp-deficiencies.md) — открытые проблемы, приоритеты и критерии закрытия.
2. [MVP-аудит](mvp-audit-2026-09-24.md) — уже внесённые исправления и краткий вердикт.
3. [Устранение недостатков](mvp-remediation-2026-09-24.md) — работа после PR #26 и оставшиеся проверки.
4. [Сверка с кейсом](case-alignment.md) — требования и критерии приёмки.
5. [API-контракт](api-contract.md) — маршруты, payload и статусы.
6. [План команды](team-work-plan.md) — зоны ответственности и GitFlow.
7. [DEV-3 handoff](dev3-handoff.md) — сдачи, проверка, куратор и последние исправления.
8. [Пользовательские потоки](user-flows.md) — экраны и поведение интерфейса.

[Backend handoff](backend-handoff.md) и
[frontend handoff](frontend-audit-handoff.md) сохраняют историческое состояние
отдельных веток до интеграции. [Журнал интеграции DEV-1](dev1-integration-log.md)
содержит решения по общим границам и запись о состоянии до объединения PR #10–#12.

Архитектурные решения верхнего уровня находятся в `../ARCHITECTURE.md`, порядок
приоритетов — в `../ROADMAP.md`, запуск — в `../README.md`.

## Точка отсчёта

Реализации DEV-1, DEV-2 и DEV-3 уже влиты в `develop`. Перед новой задачей
обновить `develop` и создать от него короткую feature-ветку.

Фактическое состояние определяется кодом; краткая матрица находится в
[сверке с кейсом](case-alignment.md). Исторические handoff не описывают
состояние текущего `develop`.

На текущем `develop` DEV-1 также поддерживает request-id наблюдаемость,
конфигурируемый upload-лимит Nginx/API, PostgreSQL backup/restore runbook и
опциональный HTTPS Compose-профиль с внешними PEM-сертификатами.
Числа тестов в исторических handoff относятся к их срезам, не к текущему
`develop`; актуальные результаты этого PR записаны в отчёте об устранении.
Live Compose/HTTPS/recovery требуют Docker Compose, daemon и целевой стенд.

## Обязательное правило координации

- DEV-1 — общая схема, миграции, `config`, `accounts`, `courses`, Compose.
- DEV-2 — `frontend/`.
- DEV-3 — `grading`, `mentoring` и браузерная проверка Python; прогресс
  рассчитывается в общем `learning.services`. Исправления и оставшаяся
  интеграционная проверка — в [DEV-3 handoff](dev3-handoff.md).
- Изменение общего API, `learning/models.py`, начальных миграций или корневых URL
  сначала фиксируется в коротком согласовании с DEV-1.
- Feature-ветки направляются только в `develop`; `main` и `develop` напрямую не
  изменяются.
