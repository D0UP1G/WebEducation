# Текущие задачи команды — 24 сентября 2026

Этот план заменяет исходную декомпозицию DEV-1/DEV-2/DEV-3: базовые функции уже в develop, а организатор передал [брендбук и учебный пакет](organizer/README.md). Источники требований — [кейс](../keys-obrazovatelnaya-platforma.pdf), эти два оригинала, [архитектура](../ARCHITECTURE.md), [роадмап](../ROADMAP.md), [API-контракт](api-contract.md), [сверка с кейсом](case-alignment.md) и [реестр недостатков](mvp-deficiencies.md). При расхождении описания и реализации проверять код и тесты, затем обновлять документацию в том же PR.

## Состояние develop после PR #45

- В develop работают три роли, девять технических типов шагов, версии курсов, сдачи, вопросы, прогресс, CI и резервирование БД/файлов; API принимает составную сдачу Scratch/Minecraft. PR #37 добавил карту и preflight импорта, PR #45 — подсказку Scratch после неверного ответа. После PR #45 локально прошли 71 backend-тест, 68 frontend-тестов, preflight 3/9/30 и production build.
- Пакет содержит три курса, девять модулей и 30 шагов: Scratch (2–4 классы), Minecraft Education (3–6), Python (5–9). Получение файлов не означает их импорт в базу: seed_demo по-прежнему создаёт только синтетический курс из шести шагов.
- Курс в БД пока имеет плоский список шагов без сущности модуля, структурированного паспорта (инструмент, цель, объём) и стабильных ключей исходного пакета. Публикация в `develop` уже требует теорию и контрольный вопрос, как требует §04 кейса.
- Исходный DOCX содержит ответы, эталонные решения и тесты с пометкой «скрытый». По решению владельца он размещён в публичном Git. Следовательно, эти тесты публичны, а эталонный код можно скопировать. Текущая браузерная сдача Python допускает подмену результата. Новое требование владельца — защищённый зачёт: локальный запуск оставить для самопроверки, официальный результат получать только от изолированного серверного запуска кода.
- PR #41 адаптировал рабочий React к дизайну и брендбуку; PR #44 уточнил маршруты и экраны по утверждённому прототипу. Визуальная проверка всех ролей на стенде ещё нужна. PR #30 добавил составную ручную сдачу, PR #36 — обязательные доказательства и закрытые критерии в серверной части. React ещё должен показывать требования шага и критерии куратору. Для остальных маршрутов React и Django соединены через `/api/v1/`.
- Нет принятого публичного HTTPS-стенда, полного браузерного E2E и релиза текущего продукта в main. CI работает, но защита main/develop на GitHub выключена.

## Общий контракт перед параллельными изменениями

DEV-1 фиксирует краткое решение в API-контракте до изменения моделей: как представлены курс → модуль → шаг, паспорт курса, исходный педагогический тип шага и необходимые поля ручной сдачи. Отдельно DEV-1 и DEV-3 согласуют протокол официальной Python-сдачи: браузер передаёт код, но не результаты тестов как источник оценки; сервер запускает код изолированно и возвращает проверенный статус. Локальный Web Worker служит только предварительной самопроверке. DEV-2, DEV-3 и DEV-4 согласуют только нужные поля. Опубликованные ревизии и старые назначения должны оставаться неизменяемыми; миграциями общих моделей владеет DEV-1. Для редактора и импорта использовать один формат данных.

**Решение владельца 24.09:** для публикации каждого курса обязательны теория и контрольный вопрос, как в §04 кейса. Прежнее разрешение курса с одной теорией отменено. Серверная валидация в `develop` уже требует оба шага; DEV-2 синхронизирует подсказки редактора. Все три курса организатора уже содержат вопросы.

## DEV-1 — ядро, контракт и стенд

**Владеет:** backend/config, backend/apps/accounts, backend/apps/courses, общими моделями и миграциями, Compose, базовыми URL, docs/api-contract.md. Изменения схемы согласовывает с DEV-3 и DEV-4.

1. **P0 · Модель реального курса.** Добавить упорядоченные модули и паспорт курса (классы уже есть; нужны инструмент, цель и объём) в draft и неизменяемую публикацию. Сохранить прежние курсы и назначения при миграции, порядок шагов внутри модулей и обратную совместимость API или согласованный переход фронтенда. Критерий: три курса пакета представимы без потери структуры; новая публикация не меняет старую ревизию.
2. **P0 · Типы и публикация.** PR #35 добавил `scratch.numeric_answer` и `artifact.project`; все девять типов пакета доступны в `develop`. Серверная публикация уже требует `theory` и хотя бы один контрольный вопрос (`quiz.single_choice`/`quiz.multiple_choice`). DEV-2 синхронизирует тексты редактора, а DEV-4 проверит публикацию импортированных курсов. Критерий: курс только с теорией не публикуется, курсы пакета проходят публикацию, контракт и редактор согласованы.
3. **P0 · Публичный стенд.** Развернуть HTTPS Compose с уникальными секретами и синтетическими аккаунтами, проверить миграции, прямое обновление /admin/courses и /admin/assignments, постоянные тома, backup и restore БД вместе с media на копии стенда. Добавить проверенный URL и команды в README. Критерий: протокол с датой, URL и результатом восстановления.
4. **P0 · Изоляция официального запуска.** Совместно с DEV-3 спроектировать и подключить отдельный исполнитель для каждого официального Python-прогона. У процесса нет сети и доступа к секретам/БД, он работает не от root, с ограничением CPU, памяти, времени, процессов, вывода и файловой системы; код не выполняется в Django/Gunicorn. Не выдавать контейнеру доступ к Docker socket хоста. Ограничить число параллельных сдач, поставить очередь и лимит частоты отправок; проверить нагрузку на выбранном хосте. Критерий: стендовые проверки выхода за лимиты, зависания и повторного запуска; ресурсы сервера не исчерпываются одной сдачей.
5. **P1, до release · GitHub.** Включить обязательные backend/frontend CI checks и review для develop и main; не допускать прямого push. Критерий: настройки GitHub проверены, пробный PR проходит защиту.

**Обновление DEV-1:** PR #32 внёс серверную проверку обязательных теории и контрольного вопроса и подсказку на странице назначений в `develop`. Публикация курса только с теорией отклоняется; оба варианта контрольного вопроса принимаются. После импорта DEV-4 проверит публикацию трёх курсов организатора, а DEV-1 продолжает модель модулей и изоляцию Python.

## DEV-2 — рабочий интерфейс и брендбук

**Владеет:** frontend/. Не менять backend-контракт без согласования с DEV-1/DEV-3.

1. **P0 · Официальный стиль.** PR #34 перенёс цвета и шрифты раздела 08 брендбука в рабочий React, состояния показываются цветом, иконкой и текстом. «Зачтено» одинаково для авто- и ручной проверки, источник проверки подписан отдельно. У ученика основной текст 18 px, кнопка следующего шага 56 px; у сотрудников более плотная шкала. Критерий завершения: три ролевых сценария читаемы на ноутбуке и мобильном экране, контраст, фокус и загрузка шрифтов проверены на стенде.
2. **P0 · Реальные курсы.** После контракта DEV-1 показать модули, паспорт и следующий шаг; формы редактора должны собирать все варианты шагов пакета. После контракта DEV-3 дать ученику приложить требуемые для Minecraft/проекта снимок, ссылку и текст; куратору показать критерии проверки, не раскрывая ответы ученику. Критерий: каждый из 30 шагов доступен и понятен в соответствующей роли.
3. **P0 · Python и сообщения.** Разделить интерфейс «Проверить локально» и «Отправить на зачёт»: первый запускает Pyodide у ученика и не начисляет баллы, второй отправляет код серверу и показывает его проверенный результат через polling. На целевом стенде проверить Pyodide; при недоступности CDN разместить runtime локально. Тексты ученику сверить с разделом 07 брендбука: коротко, на «ты», без обвинительной формулировки. Критерий: локальный результат нельзя принять за официальный, а неверный ответ, таймаут, ошибка среды и возврат различимы.
4. **P1 · Дизайн-прототип.** Сравнить design/webeducation-prototype.html с официальным брендбуком и переносить только совместимые решения. Не добавлять лидерборд или серии ради макета: кейс требует объяснимый рейтинг, который уже считается по баллам.

## DEV-3 — проверка, ручные работы и границы доверия

**Владеет:** backend/apps/grading, backend/apps/mentoring и логикой официальной Python-проверки; браузерную самопроверку согласует с DEV-2. Изменения общих моделей передаёт DEV-1.

1. **P0 · Отображение педагогических типов.** Для Scratch с ответом числом использовать серверный точный ответ с сохранением контекста Scratch; для «Проекта» обеспечить ручную сдачу как отдельный тип или документированную совместимую разновидность. Проверить, что все 30 шагов пакета отображаются без потери механизма проверки. Критерий: таблица соответствия исходный шаг → type_key и тесты каждого механизма.
2. **P0 · Несколько доказательств и критерии куратора.** PR #30 добавил серверный приём файла, ссылки и пояснения в одной попытке для Scratch/Minecraft; подключить поля в форме, задать обязательные доказательства для заданий пакета и хранить критерии так, чтобы куратор их видел, а ученик не получал эталонные ответы. Критерий: отправка, возврат, повтор и принятие работы из пакета проходят API-тест и браузерный сценарий.
3. **P0 · Защищённая Python-сдача.** С DEV-1 заменить доверие к присланному stdout/времени/памяти на официальный серверный прогон присланного кода в изоляции. Сервер сам выбирает тесты, измеряет ресурсы и вычисляет статус/баллы; результаты из браузера игнорирует или отвергает. Сохранить локальный Pyodide только для самопроверки. Проверить импортированные входы, ожидаемый вывод и 1 с / 256 МБ из паспортов задач; сохранить переносы строк внутри таблиц DOCX, особенно в задаче «Парты». Критерий: поддельный stdout не даёт зачёт, таймаут/память/вывод ограничены на сервере, ошибки среды отделены от неверного ответа, есть тесты и handoff. Публичные решения DOCX означают, что защита от списывания остаётся отдельной задачей.
4. **P1 · Безопасность вложений.** Сохранить download как attachment; оценить глубокую проверку PDF/архивов и предупреждение куратора из реестра MVP-16. Не расширять форматы без необходимого сценария пакета.

**Обновление DEV-3:** PR #35 добавил все девять типов; PR #36 проверил `required_evidence` и скрыл `review_criteria` от ученика, закреплённый куратор получает критерии в очереди и карточке проверки. PR #45 переносит подсказку Scratch 1.1.3 из карты в закрытое содержимое шага и возвращает только после неверной сдачи. DEV-2 добавляет поля редактора и ролевое отображение в React; DEV-4 проверяет подсказку на импортированном курсе. Официальный Python-исполнитель не реализован.

## DEV-4 — перенос содержания, интеграция и выпуск

**Владеет:** переносом учебного содержания, матрицей соответствия, сквозной приёмкой и актуальными docs. Создание моделей и API оставляет DEV-1, обработчики проверки — DEV-3, frontend — DEV-2.

**Статус после PR #40:** в `develop` есть [машиночитаемая карта переноса](organizer/curriculum-map.json) для 3 курсов, 9 модулей и 30 шагов. Она сохраняет исходные блоки DOCX, отделяет ученическое содержимое от ответов/критериев, сохраняет многострочные Python-входы, задаёт `required_evidence` для шести ручных шагов и по 1 баллу каждому шагу. Преобразователь и `preflight_curriculum_import` проверяют SHA-256, порядок, ключи, типы и поля; команда выводит только сводку и не меняет базу. PR #36 добавил серверную поддержку доказательств и критериев. Запись draft в БД ожидает модульную модель, поля паспорта и стабильные ключи импорта от DEV-1. Актуальные проверки — в [журнале DEV-4](dev4-integration-log.md).

1. **P0 · Подготовка содержимого — карта и баллы готовы; требуется ревью DEV-1/DEV-3.** Карта фиксирует исходный тип, целевой `type_key`, режим проверки, требуемые поля сдачи, видимый текст, ответы и критерии. Сохраняются входы/выходы и переносы строк; исходный DOCX не изменён. Владелец назначил по 1 баллу каждому шагу 24.09.2026; карта и preflight обновлены в PR #40. Остаются сущности модуля, поля паспорта, стабильные ключи импорта и сквозная проверка подсказки Scratch после импорта.
2. **P0 · Воспроизводимый импорт — преобразователь и preflight готовы; запись в БД ждёт схему DEV-1 и DEV-3.** Реализовать идемпотентный импорт трёх курсов в draft, опубликовать версии, назначить синтетические аккаунты; не затирать существующий seed_demo или работы. Критерий: повторный импорт не создаёт дублей, структура 3/9/30 и все ответы сохраняются, публичное API скрывает ключи ответов.
3. **P0 · Сквозная приёмка.** Провести на HTTPS URL путь администратор → ученик → куратор → прогресс с реальным курсом из пакета: теория, один/несколько ответов, Scratch, Minecraft, защищённый Python, проект, возврат и пересдача, затем новая версия без изменения старого назначения. Отдельно отправить поддельные клиентские результаты при неверном коде и проверить отказ в зачёте; проверить лимиты исполнителя. Записать URL, дату, коммит, каждый результат и `meta.request_id`/`X-Request-ID` соответствующих API-запросов без ответов и персональных данных в журнале; при возможности автоматизировать критические переходы. Критерий: сценарий воспроизводится другим участником по README.
4. **P0 · Документы и release.** После каждого PR обновлять текущий план, сверку с кейсом и соответствующий handoff, отделяя исторические срезы. После зелёного CI и приёмки открыть release/* PR из develop в main, затем синхронизировать результат обратно в develop. Критерий: default branch показывает принятый продукт до стоп-кода.

## Порядок и GitFlow

1. Параллельно: DEV-1 пишет контракт схемы и исполнителя, готовит стенд; DEV-2 переносит токены и разделяет локальную/официальную проверку; DEV-3 проверяет типы/вложения и проектирует официальный grading; DEV-4 составляет карту содержания.
2. После контракта: DEV-1 объединяет модели, API и изоляцию в develop; DEV-3 — проверки; DEV-2 — формы и модули; DEV-4 — импорт и E2E.
3. Любая feature-ветка создаётся от свежего develop, один небольшой PR на результат, цель PR — develop. Общие модели и миграции меняет DEV-1; конфликты решать до merge. Тесты и краткий лог проверки обязательны в PR.
4. Стоп-код по кейсу — 26 сентября 2026, 09:00. Сначала принимаются реальные курсы и рабочий URL, затем визуальная полировка и P1. Main обновляется только проверенным release PR.
